import java.util.* // import statement needed
public class GitDemo
{
    public static void main (String[] args)
    {
        Scanner scan = new Scanner();
        int first = scan.nextInt(); // Change to NextInt()
        int second = scan.nextInt(); // Change to NextInt()
        System.out.println((first + second) / 2); // First + second needed parentheses
    }
}
